---
export interface Props {
    logo: string;
}
const { logo } = Astro.props
---

<img class="logo" src={`/tomas-arcostanzo/logos/${logo}.svg`} alt="logos"/>

<style>
    .logo {
        opacity: .5;
        width: 5rem;
        height: 5rem;
    }
</style>